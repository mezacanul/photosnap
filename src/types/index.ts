interface FeatureProps {
    image: string;
    title: string;
    description: string;
}

interface StoryProps {
    image: string;
    title: string;
    author: string;
}

export type { FeatureProps, StoryProps };
